{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "1.2.schema.json",
  "type": "object",
  "minProperties": 1,
  "additionalItems": true,
  "properties": {
    "id": {
      "type": "string",
      "description": "A unique identifier for the file. This is used for references to dictionaries."
    },
    "version": {
      "description": "The schema version for Rosetta. E.G: 1.2",
      "type": "string"
    },
    "locale": {
      "type": "string",
      "description": "The language locale to identify the language used for documentation. (Default: EN_US)",
      "default": "EN_US"
    },
    "languages": {
      "type": "object",
      "minProperties": 1,
      "properties": {
        "java": {
          "$ref": "1.2/java.json"
        },
        "lua": {
          "$ref": "1.2/lua.json"
        }
      }
    },
    "applications": {
      "type": "object",
      "description": "All third-party schema definitions goes here."
    },
    "tags": {
      "$ref": "1.2/tags.json"
    }
  },
  "required": ["version", "id"]
}
